import { Box, Heading } from "@chakra-ui/react";
import React from "react";

const ContactUs = () => {
  return (
    <Box padding={2} overflow={"scroll"}>
      <Heading padding={2}>Contact Us</Heading>
    </Box>
  );
};

export default ContactUs;
